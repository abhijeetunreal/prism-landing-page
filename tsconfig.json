{
  "compilerOptions": {
    "rootDir": ".",   // Allow root directory as source
    "outDir": "./dist",
    // ... other options
  },
  "include": ["src"], // Explicitly include only the `src` folder
  "exclude": ["node_modules", "dist"]
}
